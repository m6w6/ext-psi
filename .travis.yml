# autogenerated file; do not edit
language: c
sudo: false

addons:
 apt:
  packages:
   - php5-cli
   - php-pear
   - re2c

env:
 matrix:
  - PHP=7.0 enable_debug=no enable_psi=yes enable_psi_posix=all
  - PHP=7.1 enable_debug=no enable_psi=yes enable_psi_posix=all
  - PHP=master enable_debug=no enable_psi=yes enable_psi_posix=all
  - PHP=7.0 enable_debug=yes enable_psi=yes enable_psi_posix=all
  - PHP=7.1 enable_debug=yes enable_psi=yes enable_psi_posix=all
  - PHP=master enable_debug=yes enable_psi=yes enable_psi_posix=all

before_script:
 - make -f travis/pecl/Makefile php
 - make -f travis/pecl/Makefile ext PECL=psi

script:
 - make -f travis/pecl/Makefile test

after_failure:
 - cat config.log
